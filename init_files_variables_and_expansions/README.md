# Shell, init files, variables and expansions

This project covers shell initialization files, environment variables, local and global variables, aliases, shell expansions, command substitution, and shell arithmetic.
